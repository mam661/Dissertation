So, to start make it work, we need to build/ open in a container

https://docs.ros.org/en/jade/api/mavlink/html/mavutil_8py_source.html

export GAZEBO_MODEL_PATH=/workspaces/Dissertation/models:$GAZEBO_MODEL_PATH
gazebo --verbose /workspaces/Dissertation/models/map2.world

Then in another terminal:

source ~/.bashrc

python3 spawn_drones.py --drones 2
^^The number at the end is the number of drones


Then this (Mentioned in the console after the above command):
cd /ardupilot && sim_vehicle.py -v ArduCopter -f gazebo-iris -I0 --console --add-param-file=/workspaces/Dissertation/sitl_params.parm
^^ The -I[number] denotes which drone is being controlled

//then wait a bit for it all to sync and stuff, and then:

mode guided

arm throttle

takeoff 5


Then there can be:

velocity x y z, where it says for how long to go in each direction



pkill -9 gzserver; pkill -9 gzclient; pkill -9 gazebo; pkill -9 python3




output add 127.0.0.1:14551